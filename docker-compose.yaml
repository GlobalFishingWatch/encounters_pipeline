version: '2'
services:
  pipeline:
    image: gfw/pipe-encounters
    build: .
    volumes:
      - ".:/opt/project"
      - "gcp:/root/.config/"

  gcloud:
    image: gfw/pipe-encounters
    build: .
    entrypoint: gcloud
    volumes:
      - "gcp:/root/.config/"

volumes:
  gcp:


